import React from 'react';
import {StudentCards} from "@components/cards/studentCard/StudentCard";

const Main = () => {
    return (
        <div>
            <StudentCards />
        </div>
    );
};

export default Main;
